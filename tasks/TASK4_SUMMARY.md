# Задание 4: Кеширование - Итоговая сводка

> 🚀 Полное описание выполнения Задания 4  
> 🏠 [← Вернуться к README](../README.md)

## ✅ Задача

Добавить Redis кеширование согласно третьей схеме из планирования:
- Добавить Redis в `compose.yaml`
- Настроить приложение для использования Redis
- Обеспечить ускорение повторных запросов (<100мс)
- Показать метрики кеширования в API

## ✅ Что было сделано

### 1. Модифицирован compose.yaml

**Имя проекта:** `sharding-repl-cache`

**Инфраструктура (12 контейнеров):**

#### Redis Cache (1)
```yaml
redis
- Порт: 6379
- Команда: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
- Политика вытеснения: allkeys-lru (удаление редко используемых ключей)
```

#### Config Servers Replica Set (3)
```yaml
configSrv1, configSrv2, configSrv3
```

#### Shard 1 Replica Set (3)
```yaml
shard1-1, shard1-2, shard1-3
```

#### Shard 2 Replica Set (3)
```yaml
shard2-1, shard2-2, shard2-3
```

#### Mongos Router (1)
```yaml
mongos
```

#### Application (1)
```yaml
pymongo-api
- Добавлено: REDIS_URL: "redis://redis:6379"
- depends_on: mongos, redis
```

### 2. Настроено кеширование в приложении

**Переменная окружения:**
```yaml
REDIS_URL: "redis://redis:6379"
```

**Кешируемые endpoints:**
- `GET /{collection_name}/users` - список пользователей (TTL: 60 секунд)

**Как работает:**
```python
@app.get("/{collection_name}/users")
@cache(expire=60 * 1)  # Кеширование на 60 секунд
async def list_users(collection_name: str):
    time.sleep(1)  # Симуляция медленного запроса
    collection = db.get_collection(collection_name)
    return UserCollection(users=await collection.find().to_list(1000))
```

**Логика:**
1. **Первый запрос**: Данные читаются из MongoDB (~1 секунда), сохраняются в Redis
2. **Повторные запросы**: Данные берутся из Redis (~10-50мс)
3. **После TTL**: Кеш истекает, снова читаем из MongoDB

### 3. API показывает статус кеширования

```json
{
  "cache_enabled": true,
  ...
}
```

## 🚀 Как запустить и проверить

### Быстрый старт

```bash
# 1. Запуск контейнеров (12 шт)
docker compose up -d

# 2. Инициализация (используем тот же скрипт)
./scripts/init-replication.sh

# 3. Проверка базовой работы
curl http://127.0.0.1:8080 | jq
```

### Проверка кеширования

#### 1. Проверка, что Redis включен

```bash
curl -s http://127.0.0.1:8080 | jq '.cache_enabled'
```

**Ожидается:** `true`

#### 2. Первый запрос (без кеша, медленный)

```bash
time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
```

**Ожидается:** ~1000-1200мс (медленно, читаем из MongoDB)

#### 3. Второй запрос (из кеша, быстрый)

```bash
time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
```

**Ожидается:** ~10-50мс (быстро, читаем из Redis) ✅

#### 4. Третий запрос (из кеша, быстрый)

```bash
time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
```

**Ожидается:** ~10-50мс (быстро, из Redis)

### Детальное тестирование производительности

```bash
# Функция для измерения времени
test_cache() {
  echo "Тест кеширования для /helloDoc/users"
  echo "========================================"
  
  echo -n "Запрос 1 (без кеша): "
  time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
  
  echo -n "Запрос 2 (из кеша):  "
  time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
  
  echo -n "Запрос 3 (из кеша):  "
  time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
  
  echo -n "Запрос 4 (из кеша):  "
  time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
  
  echo "========================================"
  echo "Разница: ~20x ускорение!"
}

test_cache
```

**Ожидаемый результат:**
```
Тест кеширования для /helloDoc/users
========================================
Запрос 1 (без кеша): 1.2s
Запрос 2 (из кеша):  0.05s
Запрос 3 (из кеша):  0.03s
Запрос 4 (из кеша):  0.04s
========================================
Разница: ~20x ускорение!
```

### Проверка Redis

#### Проверка подключения к Redis
```bash
docker compose exec redis redis-cli ping
```

**Ожидается:** `PONG`

#### Просмотр ключей в Redis
```bash
docker compose exec redis redis-cli KEYS '*'
```

**Ожидается:** Список ключей вида `api:cache:*`

#### Проверка содержимого кеша
```bash
docker compose exec redis redis-cli GET "api:cache:<key>"
```

#### Очистка кеша
```bash
docker compose exec redis redis-cli FLUSHALL
```

После очистки первый запрос снова будет медленным.

#### Статистика Redis
```bash
docker compose exec redis redis-cli INFO stats
```

## ✅ Соответствие требованиям ревьюера

### ✓ Проект запускается
```bash
docker compose up -d
```
**Результат:** 12 контейнеров в статусе running

### ✓ Настройка выполняется без ошибок
```bash
./scripts/init-replication.sh
```
**Результат:** Инициализация проходит успешно

### ✓ Приложение показывает общее количество документов
```bash
curl -s http://127.0.0.1:8080 | jq '.collections.helloDoc.documents_count'
```
**Результат:** 1000

### ✓ Приложение показывает количество в каждом шарде
```bash
curl -s http://127.0.0.1:8080 | jq '.shard_distribution.helloDoc'
```
**Результат:** Распределение по shard1ReplSet и shard2ReplSet

### ✓ Приложение показывает количество реплик
```bash
curl -s http://127.0.0.1:8080 | jq '.shards'
```
**Результат:** По 3 реплики в каждом шарде

### ✓ Второй и последующие вызовы <100мс
```bash
# Первый запрос
time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
# ~1000мс

# Второй запрос
time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
# ~30-50мс ✅ (<100мс)

# Третий запрос
time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
# ~30-50мс ✅ (<100мс)
```

## 📊 Архитектура

### Компоненты

| Компонент | Количество | Порт | Назначение |
|-----------|------------|------|------------|
| Config Servers | 3 | 27019 | Метаданные (replica set) |
| Shard 1 Replicas | 3 | 27018 | Данные (replica set) |
| Shard 2 Replicas | 3 | 27018 | Данные (replica set) |
| Mongos Router | 1 | 27017 | Маршрутизация |
| **Redis** | **1** | **6379** | **Кеширование** |
| API Application | 1 | 8080 | HTTP API |
| **Всего** | **12** | | |

### Архитектура с кешированием

```
Client
    ↓
pymongo-api:8080
    ├─→ Redis:6379 (Cache Check)
    │   └─ Cache Hit → Быстрый ответ (10-50мс)
    │   └─ Cache Miss ↓
    │
    └─→ mongos:27017 (Database Query)
        ├─→ Config Servers (Metadata)
        ├─→ Shard 1 Replicas (Data)
        └─→ Shard 2 Replicas (Data)
            └─ Результат → Сохранить в Redis → Ответ (~1000мс)
```

### Преимущества финальной архитектуры

- ✅ **Производительность**: Redis кеш ускоряет повторные запросы в ~20 раз
- ✅ **Отказоустойчивость**: Replica sets обеспечивают automatic failover
- ✅ **Масштабируемость**: Шардирование распределяет данные
- ✅ **Снижение нагрузки**: Кеш разгружает MongoDB
- ✅ **Готовность к высокой нагрузке**: Комбинация всех оптимизаций

## 🔧 Настройки кеширования

### Политика вытеснения

В `compose.yaml` настроена политика `allkeys-lru`:
```yaml
command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
```

**Что это означает:**
- `maxmemory 256mb` - максимум 256MB для кеша
- `allkeys-lru` - при заполнении памяти удаляются редко используемые ключи (Least Recently Used)

### TTL (Time To Live)

В приложении кеш живет 60 секунд:
```python
@cache(expire=60 * 1)  # 60 секунд
```

Можно изменить в `api_app/app.py`.

### Ключи кеша

FastAPI Cache генерирует ключи автоматически на основе:
- URL endpoint
- Query параметров
- Prefix: `api:cache:`

## 🧪 Тестирование

### Тест 1: Базовое кеширование

```bash
# Очистить кеш
docker compose exec redis redis-cli FLUSHALL

# Первый запрос (медленно)
time curl -s http://127.0.0.1:8080/helloDoc/users | jq '.users | length'

# Второй запрос (быстро)
time curl -s http://127.0.0.1:8080/helloDoc/users | jq '.users | length'
```

### Тест 2: Истечение TTL

```bash
# Запрос (создать кеш)
curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null

# Подождать 65 секунд (TTL = 60 сек + запас)
sleep 65

# Запрос снова будет медленным
time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
```

### Тест 3: Множественные запросы

```bash
# 10 запросов подряд
for i in {1..10}; do
  echo -n "Запрос $i: "
  time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null 2>&1
done
```

**Ожидается:** Первый медленный (~1s), остальные быстрые (~50ms)

### Тест 4: Параллельные запросы

```bash
# 5 параллельных запросов
for i in {1..5}; do
  curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null &
done
wait

echo "Все запросы завершены"
```

## 🔍 Мониторинг кеша

### Статистика Redis

```bash
docker compose exec redis redis-cli INFO stats | grep -E "hits|misses"
```

**Показывает:**
- `keyspace_hits` - успешные попадания в кеш
- `keyspace_misses` - промахи (данных нет в кеше)

**Hit Rate = hits / (hits + misses)**

### Количество ключей

```bash
docker compose exec redis redis-cli DBSIZE
```

### Использование памяти

```bash
docker compose exec redis redis-cli INFO memory | grep used_memory_human
```

## ✅ Критерии выполнения

- [x] Все 12 контейнеров запущены
- [x] Redis добавлен и работает
- [x] REDIS_URL настроен в приложении
- [x] Коллекция `helloDoc` содержит ≥1000 документов
- [x] Приложение показывает распределение по шардам
- [x] Приложение показывает количество реплик
- [x] Первый запрос к `/helloDoc/users` ~1000мс
- [x] Второй и последующие запросы <100мс ✅
- [x] `cache_enabled` = true в API
- [x] Redis сохраняет данные корректно

## 📚 Связанная документация

- 🔧 [TASK4_CACHING_SETUP.md](TASK4_CACHING_SETUP.md) - подробная настройка
- 📖 [TASK1_PLANNING.md](TASK1_PLANNING.md) - планирование (Схема 3)
- 📊 [diagrams/ARCHITECTURE.md](../diagrams/ARCHITECTURE.md) - схема 3
- 🏠 [README.md](../README.md) - главная страница

## ✅ Статус

**Задание 4 выполнено на 100%**

Все требования ревьюера соблюдены:
- ✅ Проект запускается (12 контейнеров)
- ✅ Настройка проходит без ошибок
- ✅ Приложение показывает все метрики
- ✅ Повторные запросы выполняются <100мс
- ✅ Кеширование работает корректно

**Готово к проверке!**

**Итоговая архитектура:** Шардирование + Репликация + Кеширование = Готовность к "черной пятнице"! 🎉

