# Задание 4: Запуск и проверка кеширования

> 📋 Как запустить систему с Redis и проверить работу кеширования  
> 🏠 [← Вернуться к README](../README.md) | 📊 [Сводка задания](TASK4_SUMMARY.md) | 🎨 [Архитектура](../diagrams/ARCHITECTURE.md)

---

## 🚀 Быстрый старт

```bash
# Перейти в директорию проекта
cd sharding-repl-cache

# 1. Запуск всех контейнеров (12 шт: 3 config + 6 shards + mongos + redis + api)
docker compose up -d

# 2. Инициализация MongoDB (подождите 10-15 секунд)
./scripts/init-replication.sh

# 3. Проверка базовой работы
curl http://127.0.0.1:8080 | jq

# 4. Тест производительности кеширования
./scripts/test-cache.sh
```

---

## 📊 Что запускается

**12 контейнеров:**
- 3 Config Servers (configSrv1, configSrv2, configSrv3) - метаданные кластера
- 6 Shard Replicas (shard1-1/2/3, shard2-1/2/3) - данные с репликацией
- 1 Mongos Router - маршрутизация запросов
- 1 **Redis** - кеширование запросов ⭐
- 1 API приложение (pymongo-api)

---

## 📝 Шаг 1: Запуск контейнеров

```bash
docker compose up -d
```

**Проверка статуса:**

```bash
docker compose ps
```

**Все 12 контейнеров должны быть в статусе `running`.**

---

## 📝 Шаг 2: Инициализация MongoDB

```bash
./scripts/init-replication.sh
```

**Что делает скрипт:**
- Инициализирует replica sets для config servers (3 ноды)
- Инициализирует replica sets для каждого шарда (по 3 ноды)
- Добавляет шарды в mongos
- Включает шардирование для БД `somedb`
- Создает коллекцию `helloDoc` и шардирует ее по `_id`
- Вставляет 1000 тестовых документов

**Ожидаемый результат:**

```json
{
  "mongo_topology_type": "Sharded",
  "collections": {
    "helloDoc": {
      "documents_count": 1000
    }
  },
  "shards": {
    "shard1ReplSet": "shard1-1:27018,shard1-2:27018,shard1-3:27018",
    "shard2ReplSet": "shard2-1:27018,shard2-2:27018,shard2-3:27018"
  },
  "cache_enabled": true,  ← ✅ Redis подключен!
  "status": "OK"
}
```

---

## 📝 Шаг 3: Проверка кеширования

### Проверка 1: Redis включен

```bash
curl -s http://127.0.0.1:8080 | jq '.cache_enabled'
```

**Ожидается:** `true` ✅

### Проверка 2: Redis работает

```bash
docker compose exec redis redis-cli ping
```

**Ожидается:** `PONG` ✅

### Проверка 3: Тест производительности

**Ручной тест:**

```bash
# Очистить кеш для чистоты эксперимента
docker compose exec redis redis-cli FLUSHALL

# Первый запрос (БЕЗ кеша - медленно)
time curl -s http://127.0.0.1:8080/helloDoc/users | jq '.users | length'
# Ожидается: ~1.2 секунды

# Второй запрос (ИЗ кеша - быстро)
time curl -s http://127.0.0.1:8080/helloDoc/users | jq '.users | length'
# Ожидается: ~0.03-0.05 секунды ✅ (<100мс)

# Третий запрос (ИЗ кеша - быстро)
time curl -s http://127.0.0.1:8080/helloDoc/users | jq '.users | length'
# Ожидается: ~0.03-0.05 секунды ✅
```

**Автоматический тест:**

```bash
./scripts/test-cache.sh
```

**Ожидаемый вывод:**

```
================================================
    Redis Caching Performance Test
================================================

✅ API is running and Redis is enabled

🧹 Clearing Redis cache for clean test...

================================================
Test 1: First request (WITHOUT cache)
================================================
Expected time: ~1.0-1.2 seconds (slow)

Running...
✅ Documents fetched: 1000
⏱️  Time taken: 1.234s

================================================
Test 2: Second request (FROM cache)
================================================
Expected time: <0.1 seconds (fast)

Running...
✅ Documents fetched: 1000
⏱️  Time taken: 0.045s

================================================
Test 3: Third request (FROM cache)
================================================
Expected time: <0.1 seconds (fast)

Running...
✅ Documents fetched: 1000
⏱️  Time taken: 0.032s

================================================
                   SUMMARY
================================================

Request 1 (no cache):  1.234s
Request 2 (cached):    0.045s
Request 3 (cached):    0.032s

🚀 Speedup: 27.4x faster with cache!

================================================
         Requirement Check (< 100ms)
================================================

Requirement: Second and subsequent requests < 100ms

✅ PASSED: All cached requests are < 100ms
   Request 2: 0.045s (< 0.1s) ✅
   Request 3: 0.032s (< 0.1s) ✅

================================================
              Redis Statistics
================================================

📊 Cached keys: 1
🔑 Cache keys:
   api:cache:list_users:helloDoc

📈 Cache stats:
   keyspace_hits:2
   keyspace_misses:1

================================================
✅ Cache performance test completed successfully!
================================================
```

---

## 📊 Мониторинг Redis

### Просмотр ключей в кеше

```bash
docker compose exec redis redis-cli KEYS '*'
```

**Пример вывода:**
```
1) "api:cache:list_users:helloDoc"
```

### Проверка содержимого кеша

```bash
docker compose exec redis redis-cli GET "api:cache:list_users:helloDoc"
```

**Покажет JSON с закешированными данными.**

### Количество ключей

```bash
docker compose exec redis redis-cli DBSIZE
```

### Статистика попаданий/промахов

```bash
docker compose exec redis redis-cli INFO stats | grep -E "keyspace_hits|keyspace_misses"
```

**Пример:**
```
keyspace_hits:10
keyspace_misses:1
```

**Hit Rate:** 10/(10+1) = 90.9% ✅

### Использование памяти

```bash
docker compose exec redis redis-cli INFO memory | grep -E "used_memory_human|maxmemory_human"
```

**Пример:**
```
used_memory_human:2.34M
maxmemory_human:256M
```

### TTL ключа

```bash
docker compose exec redis redis-cli TTL "api:cache:list_users:helloDoc"
```

**Показывает оставшееся время жизни в секундах (максимум 60).**

---

## ✅ Критерии проверки для ревьюера

### 1. Проект запускается

```bash
docker compose ps
```

**Результат:** 12 контейнеров в статусе `running` ✅

### 2. Настройка выполняется без ошибок

```bash
./scripts/init-replication.sh
```

**Результат:** Успешная инициализация, никаких ошибок ✅

### 3. Приложение показывает общее количество документов

```bash
curl -s http://127.0.0.1:8080 | jq '.collections.helloDoc.documents_count'
```

**Результат:** `1000` ✅

### 4. Приложение показывает количество в каждом шарде

```bash
curl -s http://127.0.0.1:8080 | jq '.shard_distribution.helloDoc'
```

**Результат:** Распределение по shard1ReplSet и shard2ReplSet ✅

### 5. Приложение показывает количество реплик

```bash
curl -s http://127.0.0.1:8080 | jq '.shards'
```

**Результат:** По 3 реплики в каждом шарде ✅

### 6. Второй и последующие вызовы <100мс ⭐

```bash
# Первый запрос
time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
# ~1200ms

# Второй запрос
time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
# ~30-50ms ✅ (<100мс)
```

**Результат:** Ускорение в ~20-30 раз! ✅

---

## 🧪 Дополнительные тесты

### Тест истечения TTL (60 секунд)

```bash
# Создать кеш
curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null

# Проверить TTL
docker compose exec redis redis-cli TTL "api:cache:list_users:helloDoc"
# Покажет ~60

# Подождать 65 секунд
sleep 65

# Запрос снова будет медленным (кеш истек)
time curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null
# ~1200ms (как первый раз)
```

### Очистка кеша

```bash
# Очистить весь кеш
docker compose exec redis redis-cli FLUSHALL

# Удалить конкретный ключ
docker compose exec redis redis-cli DEL "api:cache:list_users:helloDoc"
```

---

## 🔧 Устранение проблем

### Проблема: cache_enabled = false

**Диагностика:**

```bash
# Проверить переменную окружения
docker compose exec pymongo-api env | grep REDIS_URL

# Проверить статус Redis
docker compose ps redis

# Проверить логи
docker compose logs pymongo-api | grep -i redis
docker compose logs redis
```

**Решение:**

```bash
# Перезапустить контейнеры
docker compose restart pymongo-api redis
```

### Проблема: Запросы все еще медленные

**Диагностика:**

```bash
# Сделать запрос
curl -s http://127.0.0.1:8080/helloDoc/users > /dev/null

# Проверить, что ключ сохранился
docker compose exec redis redis-cli KEYS '*'
# Должен появиться ключ api:cache:list_users:helloDoc
```

**Если ключа нет:**

```bash
# Проверить логи приложения
docker compose logs pymongo-api
```

### Проблема: Порт 6379 занят

**Решение:**

```bash
# macOS
brew services stop redis

# Linux
sudo systemctl stop redis
```

---

## 📈 Результаты

### Было (без кеша):

- Каждый запрос: ~1200мс
- Нагрузка на MongoDB: 100%
- Requests per second: ~1

### Стало (с Redis):

- Первый запрос: ~1200мс
- Повторные запросы: ~30-50мс ✅
- Нагрузка на MongoDB: снижена в ~20 раз
- Requests per second: ~100-200 (для кешированных запросов)

**🚀 Ускорение: ~20-30 раз для повторных запросов!**

---

## 📚 Связанные документы

- 📊 [TASK4_SUMMARY.md](TASK4_SUMMARY.md) - итоговая сводка
- 📖 [TASK1_PLANNING.md](TASK1_PLANNING.md) - планирование архитектуры
- 🎨 [diagrams/ARCHITECTURE.md](../diagrams/ARCHITECTURE.md) - схема 3
- 🏠 [README.md](../README.md) - главная страница

---

**✅ Задание 4 выполнено!**

Система теперь готова к высокой нагрузке "черной пятницы" с:
- ✅ Шардированием (масштабируемость)
- ✅ Репликацией (отказоустойчивость)
- ✅ Кешированием (производительность)
